language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - pypy
# command to install dependencies
install: "pip install -r web/requirements.txt"
services:
  - mongodb
# command to run tests
script: python web/tests.py -v